---
title: Template
date: 9999-12-31
---

## Start writing here

import Article from "@/components/Article";
import { getArticleProps } from "@/server-scripts/post_old";

export default function MDX({ children, ...rest }) {
  return (
    <Article title={title} {...rest}>
      {children}
    </Article>
  );
}

export const getStaticProps = (context) => {
  return {
    props: getArticleProps(title),
  };
};
